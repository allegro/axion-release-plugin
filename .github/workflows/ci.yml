name: Build

on:
  pull_request:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    strategy:
      fail-fast: true
      matrix:
        gradle-version: ["7.6.6", "8.0", "8.3", "8.4", "8.5", "8.6", "8.7", "8.8", "8.9", "8.10", "8.11", "8.12", "8.13"]

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
            distribution: temurin
            java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          dependency-graph: ${{(github.event.repository != null && github.ref_name == github.event.repository.default_branch) && 'generate-and-submit' || 'disabled' }}
          gradle-version: ${{ matrix.gradle-version }}

      - name: Test with Gradle
        run: gradle build

      - name: Jacoco
        if: ${{ matrix.gradle-version == '8.13' }}
        run: gradle jacocoTestReport

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        if: ${{ matrix.gradle-version == '8.13' }}
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
